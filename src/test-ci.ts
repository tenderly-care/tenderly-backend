// Simple test file to trigger CI pipeline
export function testCiPipeline(): string {
  return 'CI pipeline test successful!';
}

export function addNumbers(a: number, b: number): number {
  return a + b;
}
